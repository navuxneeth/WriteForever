version: '3.8'

services:
  picseek:
    build: .
    ports:
      - "3000:3000"
    environment:
      - GROQ_API_KEY=${GROQ_API_KEY}
      - PORT=3000
    volumes:
      - ./chat.db:/app/chat.db
    restart: unless-stopped
